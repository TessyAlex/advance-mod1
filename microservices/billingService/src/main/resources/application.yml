spring:
 application:
   name: billingservice
server:
     port: 0
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
         defaultZone: http://localhost:8762/eureka
  instance: 
    hostname: localhost     

